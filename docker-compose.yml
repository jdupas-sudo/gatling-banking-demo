version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - JWT_SECRET=gatling-demo-secret-key-change-in-prod
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
